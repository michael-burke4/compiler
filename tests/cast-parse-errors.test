// comp_err parse
// END_HEADER


let main: () -> i32 = {
	let x: i32 = 10;
	let y: i32* = cast x, i32*);
	let y: i32* = cast(, i32*);
	let y: i32* = cast(x i32*);
	let y: i32* = cast(x,);
	let y: i32* = cast(x, i32*;
	return 5;
};
