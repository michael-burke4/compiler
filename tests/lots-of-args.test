ret 9
--TEXT--
let sum: (a: i32, b: i32, c: i32, d: i32, e: i32, f: i32, g: i32, h: i32, i: i32) -> i32 = {
	return a + b + c + d + e + f + g + h + i;
};

let main: () -> i32 = {
	return sum(1, 1, 1, 1, 1, 1, 1, 1, 1);
};
